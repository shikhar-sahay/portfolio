# CLAUDE.md

This file exists for tooling compatibility. It is NOT the canonical instruction set.

**Read `AGENTS.md` (repository root) first. It is the single canonical source of agent instructions: project purpose, visual direction, design constraints, animation philosophy, information architecture, performance constraints, copy rules, and the documentation protocol.**

Then read `docs/HANDOFF.md` for current state before making any change.
